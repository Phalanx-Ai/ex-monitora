{
  "type": "object",
  "title": "Parameters",
  "required": [
    "#api_token",
    "feed_id"
  ],
  "properties": {
    "#api_token": {
      "type": "string",
      "title": "Authorization Token",
      "format": "password"
    },
    "feed_id": {
      "type": "string",
      "title": "Feed ID"
    }
  }
}